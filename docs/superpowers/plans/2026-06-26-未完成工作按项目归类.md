# 未完成工作按项目归类 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将日报「明日工作计划」与周报「未完成工作」中按 `matchKeywords` 匹配到 project-config 的任务归为「项目对接」，其余归为「开发工作」，分两个子标题各自编号展示。

**架构：** 抽出共享匹配模块 `service/project-match.js`（消除 generate-report-kf.js 与 generate-project-email.js 间的循环依赖隐患）；在 generate-report-kf.js 新增分组工具 `groupNextPlanItems`；日报/周报输出处改为「项目对接」「开发工作」两个子标题、各组独立编号、保留排序与 A8 处理人。

**技术栈：** Node.js（CommonJS）、xlsx、mocha + chai。

**规格：** [docs/superpowers/specs/2026-06-26-未完成工作按项目归类-design.md](../specs/2026-06-26-未完成工作按项目归类-design.md)

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `service/project-match.js` | 创建 | 共享 `matchProject(projectName)`，输入项目名称返回匹配的 project 配置或 `null` |
| `generate-report-kf.js` | 修改 | 引入 `matchProject`；新增 `groupNextPlanItems` 并导出；改写日报「明日工作计划」与周报「未完成工作」输出 |
| `generate-project-email.js` | 修改 | 删除本地 `matchProject`，改 require 共享模块；保留 `matchProject` re-export |
| `test/project-match.test.js` | 创建 | `matchProject` 单元测试 |
| `test/report-grouping.test.js` | 创建 | `groupNextPlanItems` 分组测试 |

---

## 任务 1：创建共享匹配模块 `service/project-match.js`

**文件：**
- 创建：`service/project-match.js`
- 测试：`test/project-match.test.js`

- [ ] **步骤 1：编写失败的测试**

创建 `test/project-match.test.js`：

```js
var assert = require('chai').assert;
var { matchProject } = require('../service/project-match');

describe('matchProject()', function () {

  it('福建电力项目名称命中', function () {
    var p = matchProject('福建电力职业技术学院2025年智慧财务软件实施项目');
    assert.isNotNull(p);
    assert.equal(p.displayName, '福建电力职业技术学院2025年智慧财务软件实施项目');
  });

  it('沈阳大学项目名称命中（displayName 为人工简称，Excel 实际值含 matchKeyword）', function () {
    var p = matchProject('辽宁省沈阳大学智慧财务一体化实施服务项目');
    assert.isNotNull(p);
    assert.equal(p.displayName, '辽宁沈阳大学项目');
  });

  it('个旧项目名称命中', function () {
    var p = matchProject('个旧市智能支出服务与监督管理平台实施项目');
    assert.isNotNull(p);
    assert.equal(p.displayName, '云南个旧项目');
  });

  it('反兴奋剂中心项目名称命中', function () {
    var p = matchProject('国家体育总局反兴奋剂中心电子凭证报销系统实施项目');
    assert.isNotNull(p);
    assert.equal(p.displayName, '国家体育总局反兴奋剂中心');
  });

  it('非对接项目返回 null', function () {
    assert.isNull(matchProject('内部开发'));
    assert.isNull(matchProject('自主优化'));
    assert.isNull(matchProject('一体化核算内部支持项目'));
  });

  it('空值/未填写返回 null', function () {
    assert.isNull(matchProject(''));
    assert.isNull(matchProject(null));
    assert.isNull(matchProject(undefined));
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx mocha test/project-match.test.js`
预期：FAIL，报错 `Cannot find module '../service/project-match'`

- [ ] **步骤 3：编写最少实现代码**

创建 `service/project-match.js`：

```js
'use strict';

const projectConfig = require('../config/project-config');

/**
 * 根据项目名称匹配到项目配置
 * 与 generate-project-email.js 口径一致：命中任一 matchKeyword 即匹配
 * @param {string} projectName - Excel 中的项目名称
 * @returns {object|null} 匹配到的项目配置
 */
function matchProject(projectName) {
  const name = String(projectName || '');
  for (const project of projectConfig.projects) {
    if (project.matchKeywords.some((kw) => name.includes(kw))) {
      return project;
    }
  }
  return null;
}

module.exports = { matchProject };
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx mocha test/project-match.test.js`
预期：PASS，6 个用例全绿

- [ ] **步骤 5：Commit**

```bash
git add service/project-match.js test/project-match.test.js
git commit -m "功能：抽出共享 matchProject 模块，按 matchKeywords 匹配项目对接"
```

---

## 任务 2：generate-project-email.js 改用共享模块

**文件：**
- 修改：`generate-project-email.js`（顶部 require 与本地 matchProject 定义、module.exports）

- [ ] **步骤 1：确认现有测试仍覆盖（无新测试，验证回归）**

现有 [test/project-email.test.js](../test/project-email.test.js) 只测 `dedupeByTicketNo`，不测 `matchProject`。本次为纯重构，行为不变，运行现有测试作为回归基线。

运行：`npx mocha test/project-email.test.js`
预期：PASS（5 个用例）

- [ ] **步骤 2：改写 require 与删除本地定义**

在 [generate-project-email.js:21](../../generate-project-email.js#L21) 后新增一行 require：

```js
const projectConfig = require('./config/project-config');
const { matchProject } = require('./service/project-match');
```

删除本地 `matchProject` 函数（第 25-37 行的注释 + 函数定义整段删除）。

- [ ] **步骤 3：保留 module.exports 的 matchProject re-export**

[generate-project-email.js](../../generate-project-email.js) 末尾 `module.exports` 中已含 `matchProject` 条目（第 529 行）。它现在指向 require 进来的同名函数，re-export 保持向后兼容，无需改动该行——仅确认它仍在。

- [ ] **步骤 4：运行回归测试**

运行：`npx mocha test/project-email.test.js`
预期：PASS，5 个用例全绿（dedupeByTicketNo 行为不受影响）

- [ ] **步骤 5：Commit**

```bash
git add generate-project-email.js
git commit -m "重构：generate-project-email 改用共享 matchProject 模块"
```

---

## 任务 3：generate-report-kf.js 新增 `groupNextPlanItems` 分组工具

**文件：**
- 修改：`generate-report-kf.js`（顶部 require、新增函数、module.exports）
- 测试：`test/report-grouping.test.js`

- [ ] **步骤 1：编写失败的测试**

创建 `test/report-grouping.test.js`：

```js
var assert = require('chai').assert;
var { groupNextPlanItems } = require('../generate-report-kf');

function makeItem(projectName) {
  return { key: projectName, text: projectName, projectName: projectName };
}

describe('groupNextPlanItems()', function () {

  it('项目对接与开发工作正确分组', function () {
    var items = [
      makeItem('辽宁省沈阳大学智慧财务一体化实施服务项目'), // 项目对接
      makeItem('内部开发'),                                   // 开发工作
      makeItem('个旧市智能支出服务与监督管理平台实施项目'),    // 项目对接
      makeItem('自主优化'),                                   // 开发工作
    ];
    var result = groupNextPlanItems(items);
    assert.lengthOf(result.dockingItems, 2);
    assert.lengthOf(result.devItems, 2);
    assert.equal(result.dockingItems[0].projectName, '辽宁省沈阳大学智慧财务一体化实施服务项目');
    assert.equal(result.dockingItems[1].projectName, '个旧市智能支出服务与监督管理平台实施项目');
  });

  it('projectName 为空归入开发工作', function () {
    var items = [makeItem(''), makeItem(undefined), makeItem(null)];
    var result = groupNextPlanItems(items);
    assert.lengthOf(result.dockingItems, 0);
    assert.lengthOf(result.devItems, 3);
  });

  it('全部为项目对接时开发工作组为空', function () {
    var items = [
      makeItem('福建电力职业技术学院2025年智慧财务软件实施项目'),
      makeItem('国家体育总局反兴奋剂中心电子凭证报销系统实施项目'),
    ];
    var result = groupNextPlanItems(items);
    assert.lengthOf(result.dockingItems, 2);
    assert.lengthOf(result.devItems, 0);
  });

  it('空列表返回两组都为空', function () {
    var result = groupNextPlanItems([]);
    assert.lengthOf(result.dockingItems, 0);
    assert.lengthOf(result.devItems, 0);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx mocha test/report-grouping.test.js`
预期：FAIL，报错 `groupNextPlanItems is not a function`（未导出）

- [ ] **步骤 3：在 generate-report-kf.js 引入 matchProject**

在 [generate-report-kf.js:9](../../generate-report-kf.js#L9)（`require('./service/a8-service')`）后新增：

```js
const { batchQueryWorkorders, formatHandlerInfo } = require('./service/a8-service');
const { matchProject } = require('./service/project-match');
```

- [ ] **步骤 4：新增 groupNextPlanItems 函数**

在 [generate-report-kf.js](../../generate-report-kf.js) 中 `collectUniqueItems` 函数之后（约第 392 行，`collectUniqueItems` 的右花括号后空一行）插入：

```js
/**
 * 将未完成工作按项目归类拆为「项目对接」与「开发工作」两组
 * @param {Array} items - nextPlanItems（已 collectUniqueItems 去重）
 * @returns {{ dockingItems: Array, devItems: Array }}
 */
function groupNextPlanItems(items) {
  const dockingItems = [];
  const devItems = [];
  for (const item of items) {
    if (matchProject(item.projectName)) {
      dockingItems.push(item);
    } else {
      devItems.push(item);
    }
  }
  return { dockingItems, devItems };
}
```

- [ ] **步骤 5：将 groupNextPlanItems 加入 module.exports**

修改 [generate-report-kf.js:800](../../generate-report-kf.js#L800) 的 `module.exports`，在 `allowedTaskStatuses` 后、右花括号前新增一行：

```js
module.exports = {
	generateReport,
	generatePerformanceStats,
	extractDeveloperReportData,
	// 共享函数（供 generate-project-email.js 等脚本复用）
	getColumnIndexMap,
	buildRecord,
	parseDate,
	isDateInRange,
	getText,
	formatDateCN,
	allowedTaskStatuses,
	completedTaskStatuses,
	groupNextPlanItems,
};
```

- [ ] **步骤 6：运行测试验证通过**

运行：`npx mocha test/report-grouping.test.js`
预期：PASS，4 个用例全绿

- [ ] **步骤 7：Commit**

```bash
git add generate-report-kf.js test/report-grouping.test.js
git commit -m "功能：新增 groupNextPlanItems 按项目对接/开发工作分组"
```

---

## 任务 4：日报「明日工作计划」分组输出

**文件：**
- 修改：`generate-report-kf.js:633` 与 `generate-report-kf.js:667-668`（`buildReportMarkdown`）

- [ ] **步骤 1：编写失败的测试**

追加到 `test/report-grouping.test.js` 末尾（在最后一个 `describe` 之后）。由于 `buildReportMarkdown` 依赖 `reportData` 完整结构，这里以最小 `reportData` 驱动，聚焦验证「明日工作计划」分组渲染：

```js
var { buildReportMarkdown } = require('../generate-report-kf');

describe('buildReportMarkdown 未完成工作分组（day）', function () {

  function makeReportData(nextPlanItems) {
    return {
      startDate: new Date(2026, 5, 26),
      endDate: new Date(2026, 5, 26),
      demands: [], defectToDemands: [], ppDefects: [], nonPpDefects: [],
      ppInvalidDefects: [], nonPpInvalidDefects: [], noCommitDefects: [],
      commits: [], reviews: [], migrations: [], packs: [],
      achievedItems: [],
      nextPlanItems: nextPlanItems,
      inProgressDemands: [],
      monthlyDemandProgress: { month: 6, completedCount: 0, inProgressCount: 0 },
    };
  }

  function makePlanItem(key, projectName, plannedFinish) {
    return { key: key, text: key, projectName: projectName, plannedFinish: plannedFinish || '' };
  }

  it('项目对接在前、开发工作在后，各自从 1 编号', function () {
    var data = makeReportData([
      makePlanItem('A', '内部开发', ''),
      makePlanItem('B', '辽宁省沈阳大学智慧财务一体化实施服务项目', '2026年6月28日'),
    ]);
    var md = buildReportMarkdown('day', data, null);
    var dockingPos = md.indexOf('项目对接：');
    var devPos = md.indexOf('开发工作：');
    assert.isAbove(dockingPos, 0);
    assert.isAbove(devPos, 0);
    assert.isBelow(dockingPos, devPos, '项目对接应在开发工作之前');
    // 两组各自从 1. 开始
    assert.ok(/项目对接：\n\s+1\./.test(md), '项目对接应从 1 开始编号');
    assert.ok(/开发工作：\n\s+1\./.test(md), '开发工作应从 1 开始编号');
  });

  it('只有开发工作时仅显示开发工作子标题', function () {
    var data = makeReportData([makePlanItem('A', '内部开发', '')]);
    var md = buildReportMarkdown('day', data, null);
    assert.equal(md.indexOf('项目对接：'), -1);
    assert.isAbove(md.indexOf('开发工作：'), 0);
  });

  it('无未完成工作时显示暂无', function () {
    var data = makeReportData([]);
    var md = buildReportMarkdown('day', data, null);
    assert.ok(/七、明日工作计划：\n\s+暂无/.test(md));
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx mocha test/report-grouping.test.js`
预期：FAIL，新 3 个用例报错（当前仍走单列表 `buildSectionList`，无「项目对接」/「开发工作」子标题、无「暂无」）

- [ ] **步骤 3：改写日报明日工作计划输出**

修改 [generate-report-kf.js:633](../../generate-report-kf.js#L633)（`buildReportMarkdown` 内）。

原代码：
```js
	const achievedItems = collectUniqueItems(reportData.achievedItems);
	const nextPlanItems = collectUniqueItems(reportData.nextPlanItems);
```
改为：
```js
	const achievedItems = collectUniqueItems(reportData.achievedItems);
	const nextPlanItems = collectUniqueItems(reportData.nextPlanItems);
	const { dockingItems, devItems } = groupNextPlanItems(nextPlanItems);
```

原代码（[generate-report-kf.js:667-668](../../generate-report-kf.js#L667-L668)）：
```js
	markdown += `七、${nextPlanLabel}工作计划：\n`;
	markdown += buildSectionList(nextPlanItems, '    ', sortByPlannedFinishPriority, a8InfoMap);
```
改为：
```js
	markdown += `七、${nextPlanLabel}工作计划：\n`;
	if (dockingItems.length === 0 && devItems.length === 0) {
		markdown += `    暂无\n`;
	} else {
		if (dockingItems.length > 0) {
			markdown += `    项目对接：\n`;
			markdown += buildSectionList(dockingItems, '      ', sortByPlannedFinishPriority, a8InfoMap);
		}
		if (devItems.length > 0) {
			markdown += `    开发工作：\n`;
			markdown += buildSectionList(devItems, '      ', sortByPlannedFinishPriority, a8InfoMap);
		}
	}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx mocha test/report-grouping.test.js`
预期：PASS，全部用例（含新 3 个）全绿

- [ ] **步骤 5：Commit**

```bash
git add generate-report-kf.js test/report-grouping.test.js
git commit -m "功能：日报明日工作计划按项目对接/开发工作分组展示"
```

---

## 任务 5：周报「未完成工作」分组输出

**文件：**
- 修改：`generate-report-kf.js:432` 与 `generate-report-kf.js:448-449`（`buildWeekReportMarkdown`）

- [ ] **步骤 1：编写失败的测试**

追加到 `test/report-grouping.test.js` 末尾。聚焦周报「未完成工作」分组渲染：

```js
var { buildWeekReportMarkdown } = require('../generate-report-kf');

describe('buildWeekReportMarkdown 未完成工作分组（week）', function () {

  function makeWeekData(nextPlanItems) {
    return {
      month: 6,
      monthlyDemandProgress: { month: 6, completedCount: 0, inProgressCount: 0 },
      demands: [], defectToDemands: [], ppDefects: [], nonPpDefects: [],
      ppInvalidDefects: [], nonPpInvalidDefects: [],
      nextPlanItems: nextPlanItems,
    };
  }

  function makePlanItem(key, projectName, plannedFinish) {
    return { key: key, text: key, projectName: projectName, plannedFinish: plannedFinish || '' };
  }

  it('项目对接在前、开发工作在后，各自从 1 编号', function () {
    var data = makeWeekData([
      makePlanItem('A', '内部开发', ''),
      makePlanItem('B', '个旧市智能支出服务与监督管理平台实施项目', '2026年6月28日'),
    ]);
    var md = buildWeekReportMarkdown(data, null);
    var dockingPos = md.indexOf('项目对接：');
    var devPos = md.indexOf('开发工作：');
    assert.isAbove(dockingPos, 0);
    assert.isAbove(devPos, 0);
    assert.isBelow(dockingPos, devPos);
    assert.ok(/项目对接：\n\s+1\./.test(md));
    assert.ok(/开发工作：\n\s+1\./.test(md));
  });

  it('无未完成工作时显示暂无', function () {
    var data = makeWeekData([]);
    var md = buildWeekReportMarkdown(data, null);
    assert.ok(/未完成工作\n\s+暂无/.test(md));
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx mocha test/report-grouping.test.js`
预期：FAIL，新 2 个周报用例报错（当前周报仍走单列表，无子标题/暂无）

- [ ] **步骤 3：改写周报未完成工作输出**

修改 [generate-report-kf.js:432](../../generate-report-kf.js#L432)（`buildWeekReportMarkdown` 内）。

原代码：
```js
	const nextPlanItems = collectUniqueItems(reportData.nextPlanItems);
```
改为：
```js
	const nextPlanItems = collectUniqueItems(reportData.nextPlanItems);
	const { dockingItems, devItems } = groupNextPlanItems(nextPlanItems);
```

原代码（[generate-report-kf.js:448-449](../../generate-report-kf.js#L448-L449)）：
```js
	markdown += '未完成工作\n';
	markdown += buildSectionList(nextPlanItems, '', sortByPlannedFinishPriority, a8InfoMap);
```
改为：
```js
	markdown += '未完成工作\n';
	if (dockingItems.length === 0 && devItems.length === 0) {
		markdown += `  暂无\n`;
	} else {
		if (dockingItems.length > 0) {
			markdown += `  项目对接：\n`;
			markdown += buildSectionList(dockingItems, '    ', sortByPlannedFinishPriority, a8InfoMap);
		}
		if (devItems.length > 0) {
			markdown += `  开发工作：\n`;
			markdown += buildSectionList(devItems, '    ', sortByPlannedFinishPriority, a8InfoMap);
		}
	}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx mocha test/report-grouping.test.js`
预期：PASS，全部用例全绿

- [ ] **步骤 5：Commit**

```bash
git add generate-report-kf.js test/report-grouping.test.js
git commit -m "功能：周报未完成工作按项目对接/开发工作分组展示"
```

---

## 任务 6：全量回归与端到端验证

**文件：** 无（仅运行）

- [ ] **步骤 1：运行全部单元测试**

运行：`npm test`
预期：PASS，全部测试文件全绿（含原有 data-validator / email-service / index / project-email 与新增 project-match / report-grouping）

- [ ] **步骤 2：生成日报并人工核对**

运行：`npm run report:day`
预期：成功生成 `data/工作日报--前端开发部门--周兴杰--<日期>.md`。打开核对「七、明日工作计划」：进行中的对接项目任务在「项目对接」下、其余在「开发工作」下，两组各自从 1 编号。

- [ ] **步骤 3：生成周报并人工核对**

运行：`npm run report:week`
预期：成功生成 `data/工作周报--前端开发部门--周兴杰--<日期>.md`。打开核对「未完成工作」：同样按「项目对接」「开发工作」分组。

- [ ] **步骤 4：验证报告邮件逻辑未受影响**

运行：`npx mocha test/project-email.test.js`
预期：PASS（`dedupeByTicketNo` 及 `matchProject` re-export 路径正常）

- [ ] **步骤 5：（可选）端到端校验**

若本地已配置 `.env` 与腾讯文档凭证，可运行 `npm run report:project` 确认项目问题汇报邮件仍正常生成（`matchProject` 走共享模块，行为不变）。

---

## 完成标准

- [ ] `matchProject` 抽为共享模块，generate-project-email.js 与 generate-report-kf.js 复用同一实现
- [ ] 日报「明日工作计划」、周报「未完成工作」均按「项目对接」「开发工作」分组，各组独立编号、保留排序与 A8 处理人
- [ ] 空组省略子标题；两组都空时输出「暂无」
- [ ] `npm test` 全绿
- [ ] `npm run report:day` / `npm run report:week` 生成的报告人工核对分组正确
